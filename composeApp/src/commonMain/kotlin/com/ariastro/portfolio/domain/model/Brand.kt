package com.ariastro.portfolio.domain.model

/**
 * Brand identity of a shipped project. The concrete accent color is resolved in the
 * UI layer so the domain stays framework free.
 */
enum class Brand {
    MY_XL,
    SCHOOL_RYDE,
    TAG_TAG,
    SUPER_AGREE,
    TRACK_FIT,
}
